[project]
name = "python-widget"
version = "0.3.1"
description = "A Python widget library and CLI"
dependencies = ["requests>=2"]

[project.scripts]
pwidget = "pwidget.cli:main"

[build-system]
requires = ["hatchling"]
